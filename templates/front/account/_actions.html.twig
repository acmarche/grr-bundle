<a href="{{ path('grr_account_edit') }}" role="button"
   class="btn btn-primary"><i class="ti ti-pencil"></i> {{ 'label.btn.edit'|trans({}) }}</a>

<a href="{{ path('grr_front_preference_email_edit') }}" role="button"
   class="btn btn-primary"><i class="ti ti-bell"></i> {{ 'label.btn.preference.email'|trans({}) }}</a>

<a href="{{ path('grr_account_edit_password') }}" role="button"
   class="btn btn-primary"><i class="ti ti-key"></i> {{ 'label.btn.password'|trans({}) }}</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> {{ 'label.btn.delete'|trans({}) }}
</button>
<br/>
<br/>
