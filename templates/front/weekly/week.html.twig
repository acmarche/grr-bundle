{% extends '@grr_front/layout_front.html.twig' %}

{% block title %}{{ dateSelected|grrWeekNiceName }}{% endblock %}

{% block body %}

    {% include '@grr_front/navigation/_top_navigation.html.twig' %}

    <h3 class="text-center text-success">{{ dateSelected|grrWeekNiceName }}</h3>

    {#
    <div class="table-responsive">
        <table class="table table-bordered table-sm">
            <thead>
            <tr>
                <th>{{ 'ressources'|trans }}</th>
                {% for day in week.calendarDays %}
                    <th>{{ day.dayName|capitalize }} {{ day.day }}</th>
                {% endfor %}
            </tr>
            </thead>
            <tbody>
            {% set pourcent = (100/(week.calendarDays|length)+1)|round(2) %}
            {% for roomModel in roomModels %}
                {% set room = roomModel.room %}
                <tr>
                    <td>{{ room.name }}</td>
                    {% for day in roomModel.dataDays %}
                        {% include '@grr_front/weekly/_cell_day_data.html.twig' %}
                    {% endfor %}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
   #}

    {{ grrLegendTypeEntry(area) }}
{% endblock %}

{% block scripts %}
    {% include '@grr_front/navigation/area_selector/_menu_select.js.html.twig' %}
{% endblock %}
