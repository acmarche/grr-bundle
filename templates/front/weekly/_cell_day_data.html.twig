<td class="align-top" style="width: {{pourcent}}%;">
    {% for entry in day.entries %}
        {% include '@grr_front/weekly/_entry_line.html.twig' %}
    {% else %}
        <a href="{{ grrGenerateRouteAddEntry(area.id,room.id, day.day) }}" class="float-right"
           title="{{ 'label.btn.add.entry'|trans }}"> <i
                    class="fas fa-plus"></i> </a>
    {% endfor %}
</td>