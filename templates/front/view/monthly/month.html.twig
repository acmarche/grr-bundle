{% extends '@grr_front/layout_front.html.twig' %}

{% block title %}{{ dateSelected.monthName|title }} {{ dateSelected.year }}{% endblock %}

{% block body %}

    {% include '@grr_front/navigation/_top_navigation.html.twig' %}

    <h3 class="text-center text-success">
        {{ area }} - {% if room %} {{ room }} {% else %} {{ 'room.all'|trans({}) }} {% endif %}
    </h3>

    {{ monthData|raw }}

{% endblock %}
