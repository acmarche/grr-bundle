{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary" type="submit"><i class="fas fa-search"></i> {{ 'form.btn.search'|trans({}) }}</button>
{{ form_end(form) }}