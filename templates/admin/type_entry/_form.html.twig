{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary"><i class="ti ti-device-floppy"></i> {{ button_label|default('label.btn.save')|trans({}) }}</button>
{{ form_end(form) }}
