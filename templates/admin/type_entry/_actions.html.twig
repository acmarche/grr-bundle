<a href="{{ path('grr_admin_type_entry_edit', { 'id': type_entry.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-pencil"></i> {{ 'label.btn.edit'|trans({}) }}</a>

<a href="{{ path('grr_admin_type_entry_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    {{ 'back.to_list'|trans({}) }}
</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> {{ 'label.btn.delete'|trans({}) }}
</button>
<br/>
<br/>
