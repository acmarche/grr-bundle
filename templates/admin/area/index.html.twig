{% extends '@grr_admin/layout_admin.html.twig' %}

{% block title %}{{ 'area.index.title'|trans({}) }}{% endblock %}

{% block body %}
    <div class="card mb-3">
        <div class="card-header">
            <h3>{{ 'area.index.title'|trans({}) }}
                {% if is_granted('grr.area.new') %}
                    <a class="float-end btn btn-primary" href="{{ path('grr_admin_area_new') }}"><i
                                class="ti ti-plus"></i> {{ 'label.btn.area.new'| trans({}) }}</a>
                {% endif %}
            </h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th>{{ 'name'| trans }}</th>
                    <th>{{ 'area.orderDisplay'| trans }}</th>
                    <th>{{ 'area.startTime'| trans }}</th>
                    <th>{{ 'area.endTime'| trans }}</th>
                    <th>{{ 'area.rooms.title'| trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for area in areas %}
                    <tr>
                        <td>
                            <a href="{{ path('grr_admin_area_show', {'id': area.id}) }}">{{ area.name }}</a>
                        </td>
                        <td>{{ area.orderDisplay }}</td>
                        <td>{{ area.startTime }}</td>
                        <td>{{ area.endTime }}</td>
                        <td>{{ area.rooms|length }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">{{ 'records.not_found'| trans({}) }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
