<a href="{{ path('grr_admin_area_edit', { 'id': area.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-pencil"></i> {{ 'label.btn.edit'|trans({}) }}</a>

<a href="{{ path('grr_authorization_area_show', { 'id': area.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-user-shield"></i> {{ 'label.btn.authorization'|trans({}) }}</a>

<a href="{{ path('grr_admin_room_new', { 'id': area.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-plus"></i> {{ 'label.btn.room.new'|trans({}) }}</a>

<a href="{{ path('grr_admin_type_area_edit', { 'id': area.id }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-cog"></i> {{ 'label.btn.area_types'|trans({}) }}</a>

<a href="{{ path('grr_admin_area_edit', { 'id': area.id }) }}" role="button"
   class="btn btn-primary" target="apercu"><i class="ti ti-eye"></i> {{ 'label.btn.see'|trans({}) }}</a>

<a href="{{ path('grr_admin_area_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    {{ 'back.to_list'|trans({}) }}
</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> {{ 'label.btn.delete'|trans({}) }}
</button>
<br/>
<br/>
