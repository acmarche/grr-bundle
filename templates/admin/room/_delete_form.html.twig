
<!-- Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">{{ 'delete.confirm'|trans({}) }}</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                {{ 'room.delete.confirm'|trans({'name': room.name}) }}
            </div>
            <div class="modal-footer">
                <form method="post" action="{{ path('grr_admin_room_delete', {'id': room.id}) }}">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room.id) }}">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ 'label.btn.cancel'|trans({}) }}</button>
                    <button type="submit" class="btn btn-danger">{{ 'label.btn.delete'|trans({}) }}</button>
                </form>
            </div>
        </div>
    </div>
</div>