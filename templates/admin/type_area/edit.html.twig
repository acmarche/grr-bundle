{% extends '@grr_admin/layout_admin.html.twig' %}

{% block title %}{{ 'typeArea.edit.title'|trans({'name':area.name}) }}{% endblock %}

{% block body %}

    <div class="card mb-3">
        <div class="card-header">
            <h2>{{ 'typeArea.edit.title'|trans({'name':area.name}) }}</h2>
        </div>
        <div class="card-body">
            {{ include('@grr_admin/type_area/_form.html.twig') }}
        </div>
    </div>

    <br/>
    <a href="{{ path('grr_admin_area_show',{'id': area.id}) }}"><i
                class="ti ti-arrow-left"></i> {{ 'label.btn.cancel'|trans({}) }}</a>

{% endblock %}
