<a href="{{ path('grr_admin_entry_new_from_booking', { 'id': booking.id }) }}" role="button"
   class="btn btn-primary"><i class="far fa-edit"></i> {{ 'label.btn.confirm'|trans({}) }}</a>

<a href="{{ path('grr_admin_booking_index') }}" class="btn btn-primary" role='button'><i class="fas fa-list"></i>
    {{ 'back.to_list'|trans({}) }}
</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="fas fa-trash"></i> {{ 'label.btn.delete'|trans({}) }}
</button>
<br/>
<br/>
