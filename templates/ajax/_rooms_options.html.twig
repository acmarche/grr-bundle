{% if required == false %}
    <option value="">{{ 'entry.room.select.placeholder' | trans }}</option>
{% endif %}
{% for room in rooms %}
    <option value="{{ room.id }}">{{ room.name }}</option>
{% endfor %}